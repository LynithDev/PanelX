import { Component } from 'preact';

type RandomPageProps = {
    // i pressed tab to go here
}

export default class RandomPage extends Component<RandomPageProps> {
    render(props?: RandomPageProps) {
        return (
            <div>Hi</div>
        );
    }
}
